{
  "name": "miller-pay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n API,USER,ADMIN -c yellow,cyan,magenta \"npm run dev -w server\" \"npm run dev -w @millerpay/user-app\" \"npm run dev -w @millerpay/admin-app\"",
    "dev:user": "npm run dev -w @millerpay/user-app",
    "dev:admin": "npm run dev -w @millerpay/admin-app",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w @millerpay/user-app && npm run build -w @millerpay/admin-app && node scripts/prepare-vercel.mjs",
    "check": "npm run format:check && npm run build",
    "format": "prettier --write \"apps/**/*.{js,jsx,css,html,json}\" \"server/**/*.{js,json}\" \"*.{json,md}\"",
    "format:check": "prettier --check \"apps/**/*.{js,jsx,css,html,json}\" \"server/**/*.{js,json}\" \"*.{json,md}\"",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "prettier": "^3.6.2"
  }
}
